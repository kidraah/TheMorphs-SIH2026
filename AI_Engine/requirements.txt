# SIH26077 — Python Dependencies
# Install with: pip install -r requirements.txt

# ── Core Deep Learning ────────────────────────────────────────────────
# PyTorch: install manually from https://pytorch.org/get-started/locally/
# with CUDA 12.8 support for RTX 5080. Example:
#   pip install torch==2.11.0+cu128 --index-url https://download.pytorch.org/whl/cu128
# Uncomment the line below ONLY if you don't have CUDA (CPU fallback):
# torch

# ── Scientific Data Processing ────────────────────────────────────────
numpy>=1.26.0
scipy>=1.12.0
xarray>=2024.1.0
netCDF4>=1.6.5
h5py>=3.10.0
rasterio>=1.3.9
matplotlib>=3.8.0

# ── Web API & Server ──────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# ── Utilities ─────────────────────────────────────────────────────────
pillow>=10.2.0
tqdm>=4.66.0
