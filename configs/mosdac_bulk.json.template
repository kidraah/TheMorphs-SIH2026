{
  "_comment": "BULK template. DO NOT RUN until configs/mosdac_test.json has passed ingest_scan on a current-version file. Copy to mosdac_bulk.json (gitignored).",
  "_gate": "satpy's insat3d_img_l1b_h5 reader was written against older samples. If the format has drifted it fails SILENTLY -- plausible arrays, wrong values -- so bulk-pulling first risks days of unreadable data.",

  "user_credentials": {
    "username": "",
    "password": ""
  },

  "search_parameters": {
    "datasetId": "3SIMG_L1B_STD",
    "_datasetId": "Use whichever id the format check confirmed. Archive-era events (2015-2023) may need 3DIMG_L1B_STD or 3DRIMG_L1B_STD while recent dates need 3SIMG_L1B_STD.",

    "startTime": "YYYY-MM-DD",
    "endTime": "YYYY-MM-DD",

    "count": 100,
    "_count": "Ceiling is 100 per request, so an event window spanning more granules needs several requests -- paginate by day rather than expecting one call to cover a multi-day event.",

    "boundingBox": "62.3,5.8,101.7,39.5",
    "_boundingBox": "minLon,minLat,maxLon,maxLat -- LONGITUDE FIRST. These are the corner bounds of the India 4 km analysis grid (nowcast_data/grids.py). A lat-first box would silently return the wrong region.",
    "_boundingBox_mandatory": "Not an optimisation. ~400 MB/scan x 24 scans/day = ~9.6 GB/day full disk; one monsoon season is ~1.2 TB against ~271 GB free."
  },

  "_download_settings": "organize_by_date and output directory are download-client options rather than search parameters -- set them per the sample config in mdapi.zip."
}
